<!DOCTYPE html>
<html lang="ja" data-theme="light">
  <head>
    <title><%= content_for(:title) || "Kikaku Hub" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Kikaku Hub">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="flex min-h-screen flex-col bg-gradient-to-b from-base-200 to-base-100">
    <header class="navbar sticky top-0 z-50 border-b border-base-300/50 bg-base-100/90 shadow-sm backdrop-blur-md">
      <div class="mx-auto flex w-full max-w-5xl px-4">
        <div class="flex-1">
          <%= link_to "Kikaku Hub",
                      root_path,
                      class: "btn btn-ghost text-xl font-bold text-primary" %>
        </div>

        <nav class="flex-none">
          <ul class="menu menu-horizontal gap-1 p-0">
             <li>
               <%= link_to "使い方", guidance_path, class: "btn btn-ghost btn-sm" %>
             </li>
            <% if user_signed_in? %>
              <li>
                <%= link_to "マイページ", mypage_path, class: "btn btn-ghost btn-sm" %>
              </li>
              <li>
                <%= link_to "アカウント編集", edit_user_registration_path, class: "btn btn-ghost btn-sm" %>
              </li>
              <li>
                <span class="text-sm opacity-70">
                <%= current_user.nickname.presence || current_user.email %>
                <% if current_user.cohort.to_i > 0 %>
                  <span class="ml-1"><%= current_user.cohort_label %></span>
                <% end %>
                </span>
              </li>
              <li>
                <%= link_to "ログアウト",
                destroy_user_session_path,
                data: { turbo_method: :delete },
                class: "btn btn-ghost btn-sm text-error" %>
              </li>
              <% else %>
              <li>
                <%= link_to "ログイン",
                            new_user_session_path,
                            class: "btn btn-ghost btn-sm" %>
              </li>
              <li>
                <%= link_to "新規登録",
                            new_user_registration_path,
                            class: "btn btn-primary btn-sm" %>
              </li>
            <% end %>
          </ul>
        </nav>
      </div>
    </header>

    <div id="flash" class="fixed left-0 right-0 top-16 z-40">
      <%= render "shared/flash" %>
    </div>

    <main class="mx-auto w-full max-w-5xl flex-1 px-4 py-8">
      <%= yield %>
    </main>

    <footer class="border-t border-base-300 bg-base-100 py-6 text-center text-sm text-base-content/60">
      (C) 2026 Kikaku Hub
    </footer>
  </body>
</html>
