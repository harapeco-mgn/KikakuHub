<%= render_breadcrumbs([
  bc("ホーム", root_path),
  bc("テーマ一覧", themes_path),
  bc("テーマを作成")
]) %>
<div class="mx-auto max-w-2xl">
  <div class="card bg-base-100 shadow-lg ring-1 ring-base-300">
    <div class="card-body">
      <h1 class="card-title text-2xl font-bold">テーマを投稿</h1>
      <p class="text-sm text-base-content/60">やりたい企画を提案して、投票・参加表明を集めましょう。</p>

      <div class="divider"></div>

      <%= render "form", theme: @theme %>
    </div>
  </div>
</div>