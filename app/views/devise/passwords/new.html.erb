<div class="mx-auto max-w-md">
  <div class="card bg-base-100 shadow-xl ring-1 ring-base-300">
    <div class="card-body">
      <h2 class="card-title text-2xl font-bold">パスワードをお忘れですか？</h2>
      <p class="text-sm text-base-content/70">登録メールアドレスを入力してください。パスワードリセットの手順をお送りします。</p>

      <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
        <%= render "devise/shared/error_messages", resource: resource %>

        <fieldset class="fieldset mt-4">
          <%= f.label :email, class: "fieldset-legend" %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "input input-bordered w-full ring-1 ring-base-300 focus:ring-2 focus:ring-primary" %>
        </fieldset>

        <div class="mt-6">
          <%= f.submit "リセット手順を送信", class: "btn btn-primary btn-block" %>
        </div>
      <% end %>

      <div class="divider">または</div>

      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>
